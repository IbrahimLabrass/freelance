/*
this.filteredNumbers = this.trajets.filter(function (currentElement) {
    return currentElement.point_depart == trip.depart  && currentElement.point_arriver == trip.arriver;
  });
  console.log(this.filteredNumbers);
  for(var i=0;i<this.filteredNumbers.length;i++){
    this.ids[i] = this.filteredNumbers[i].uid;
    console.log(this.ids);
  }
  this.available = this.louages.filter(item => this.ids.includes(item.uid));
  console.log(this.availablelouages);
  */
 /*
 */